const VERSION = '1.0'
const filePreName = 'vendor'
const fileName = `.dll${VERSION}.js`
const fullName = `${filePreName}${fileName}`

module.exports = {
	VERSION,
	filePreName,
	fileName,
	fullName
}
